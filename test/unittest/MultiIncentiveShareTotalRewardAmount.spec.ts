import { expect } from 'chai';

describe('multi incentive on one pool, each has not-joined tick range', () => {
    
    describe('first range incentive consume all rewards', async() => {

        it('at the end of incentive, the price hasn\'t goes up to range2', () => {
            //TODO: implement this
        });
    });

    describe('second range incentive opens', async() => {

        it('first range\'s reward should fall to 1/3, when second range is opened', () => {

        });

        it('')
    })
})